# main functions of operator.qck

```text
val= struct(do(sql)){
  operation
  &&operation
}into(returns the object(*function | structure))

val = do(struct'source | q'source) {
  operation
  &&operation
} into(retVal | Struct | render)
```
## event'databasing for replay
event'sourcing {
  context(*)
  time(*)
}

Ai-memory is this
---
bitMatrix = struct'BitMatrix {
  query.sql(sqlQuery);
  &&query.mongodb(mongoDbQuery);
  &&qp.algorithm(prompt)
}
## prompt databasing into structure
```
val = v'set(do("")) {
  operation
  &&operation
})

val = do(v'set(prompt) {
  operation
  &&operation
})

```
