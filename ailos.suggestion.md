# Two functions for event'sourcing 
- event sourcing is  it is to reacreate the app in a certain time within a persons context

two "structured query functions".
time(*), context(*)


### algorithmic
do(vSet'("home last week")){

  &q'partikles(the algorithm, the fucntion is qp(algorithm))
  
  * quant'pata(structure wordplay))

}into(qt'bee or qt'b33, level of a detail)

# structured query is
do(struct'sql){
   &[mssql query]
   
   *quant isnt needed as its already apart of the mssql

}into(qt'bee or qt'b33)

# join challenge with multi database and redis and <3
do(struct'sql){

  *mssql.qck(to be joined)
   
}do(struct'sql){
  &NoSqlmngdb.qck()
}into(*hashmap)

&hashmap'struct{
and then C lives so htat one gets to build own structs again :) i miss that <3 

}
