# *qdb - the q'ant database :) 

thank you old c and memory based language people, only one i dare is linus and rk software on challenging that even a join between nosql and sql would be amazing :) 


### definitions of operators
```
*      = dereference the signal; keep the operation open
&      = final operation; emit retVal

*sql   = query through language; keep its signal live
&sql   = final language query; emit retVal
```


### Source

Q'db is designed for evolving the field or for the qt'bee doing research at home. <no one else.>

> quant is the structure you define.
> q'ant is when its undefined, like a ant gathering. 

only capitalize on structs. rest is small.

endpoint.qck('query');

#### SQL
```
do(struct'sql{
  
    * - to be joined(*endpoint.qck('sqlQuery | even a .sql'))
}into(&BitMatrix)
```

#### Nosql
do(struct'no{
    endpoint.qck('noQuery.mdb')
}into(BitMatrix(*redisEndpoint))

#### join
CarryVariable = do(struct'no{
   endpoint.qck('noQuery.mdb'))
};

carryVariable= do(struct'sql{
   endpoint.qck(
}, into(BitMatrix))

 onhold
---
```
#### The query style (v'set()) ** 

do(vSet'("home last week")){
 ** ( is it possible to query a bitmap or any structure with regular language? yes)
  &q'partikles(the algorithm, the function is qp(algorithm))

  *quant'pata(structure wordplay)

}into(qt'bee or qt'b33, level of a detail)
```



### Into()
- own structure is appreciated, challenge is any language tbh
- any driver operated leak sql networks always welcome



*bitmap
→ RoaringBitMap

*bitmap.query
→ smp_kernel
   - BitVector
   - Regex
   -own query language
   - etc

*bitmap
→ BitMatrix {
    rows,
    cols,
    words_per_row,
    data :: Vec<u64>
  }









































### algorithmic
do(vSet'("home last week")){

  &q'partikles(the algorithm, the function is qp(algorithm))

  *quant'pata(structure wordplay)

}into(qt'bee or qt'b33, level of a detail)

# structured query is
do(struct'sql){
  &[mssql query]

  *quant isnt needed as its already apart of the mssql

}into(qt'bee or qt'b33)

# join challenge with multi database and redis and <3
do(struct'sql){

  *mssql.qck(to be joined)

}do(struct'sql){
  &NoSqlmngdb.qck()
}into(retVal)

&hashmap'struct{
  and then C lives so that one gets to build own structs again :) i miss that <3
}

*bitmap
→ RoaringBitMap

*bitmap.query
→ smp_kernel
   - BitVector
   - Regex
   -own query language
   - etc

*bitmap
→ BitMatrix {
    rows,
    cols,
    words_per_row,
    data :: Vec<u64>
  }


```
hmm

* = dereference the signal
vector ≈ *

*sql = dereference the signal; that it doesnt quant into a structure yet, but kept in memory while working.
&sql is to signal this is the last operation and the next one is to build the structure.
```

